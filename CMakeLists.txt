# Add source files
set(SOURCES
    Types.h
    Logger.h
    Logger.cpp
    orderManager.cpp
    patternDetector.cpp
    candleProcessor.cpp
    scripDataReceiver.cpp
    # Add more files as needed
)

# Create a library from the source files
#add_library(MyLibrary ${SOURCES})

